公式動画(so12345等)がreadme.txtにあるブックマークレットで保存できない

削除済動画をキャッシュを利用して再生できない

非エコノミーのキャッシュがあっても、エコノミーで再生しますと表示される(実際には非エコノミーで再生されている)

エコノミーのキャッシュが既に存在しているときに、非エコノミーキャッシュが完了しても、エコノミーのキャッシュが削除されない

本家nlのapiが実装されてない

httpsが使えない
    とりあえずhttpのみプロクシを通すようにブラウザ等を設定してください。後で実装します。

サーバがContent-Lengthヘッダがついていないかつ、Connection: closeかつ、chunkedでない場合、ブラウザの読み込みが完了しない
